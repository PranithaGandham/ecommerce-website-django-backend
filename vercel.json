{
    "builds":[{
        "src": "backend/wsgi.py",
        "use": "@vercel/python",
        "config": {"maxLabdaSize": "15mb" , "runtime":"python3.11.0"}
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"backend/wsgi.py"
        }
    ]
}